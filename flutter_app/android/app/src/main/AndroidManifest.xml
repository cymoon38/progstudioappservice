<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- 광고 ID 사용 (Android 13+ 및 광고 SDK 요청 시 필요, adid 미수집 시 No Ad 발생) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <application
        tools:replace="android:label"
        android:label="그림 커뮤니티"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 애드팝콘 오퍼월 (Android는 Manifest에서 앱키·해시키·로그 사용) -->
        <meta-data android:name="adpopcorn_app_key" android:value="719270341"/>
        <meta-data android:name="adpopcorn_hash_key" android:value="f0914cb6664e4991"/>
        <meta-data android:name="enable_ap_log" android:value="enable"/>
        <!-- 애드팝콘 SSP (상용: AppKey 123870086) -->
        <meta-data android:name="adpopcorn_ssp_app_key" android:value="123870086"/>
        <!-- AdMob 미디에이션: 아래는 테스트 ID. 상용 배포 시 AdMob 콘솔/애드팝콘 사업팀 발급 ID로 교체 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>
        <!-- NAM (Naver Ad Manager) 퍼블리셔 코드: 실제 발급 코드로 교체 필요 -->
        <meta-data
            android:name="com.naver.gfpsdk.PUBLISHER_CD"
            android:value="TEST_PUBLISHER_CD"/>
    </application>
</manifest>
